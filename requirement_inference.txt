# Requirements per inferenza su Raspberry Pi
# Installazione: pip install -r requirements_inference.txt

# Core ML libraries (versioni ottimizzate per ARM)
pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.2.0

# Model persistence
joblib>=1.2.0

# Optional: Se serve scipy per alcune features
scipy>=1.10.0

# System monitoring (optional)
psutil>=5.9.0

# Lightweight logging
colorlog>=6.7.0